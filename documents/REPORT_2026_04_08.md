# DAILY REPORT - 08/04/2026

**Dự án:** Xây dựng website tìm kiếm việc làm  
**Nhóm:** [Nhóm 13]

---

## 1. Mục tiêu trong ngày

- Phát triển các API backend phục vụ hệ thống tìm kiếm việc làm
- Xây dựng giao diện trang Tìm kiếm việc làm (`timkiem.php`)
- Xây dựng giao diện trang Chi tiết công việc (`chitietcongviec.php`)

---

## 2. Tổng hợp tiến độ hôm nay

| Thành viên        | Chức năng phụ trách                                  | Trạng thái | Ghi chú                                                                   |
| ----------------- | ---------------------------------------------------- | ---------- | ------------------------------------------------------------------------- |
| [Nguyễn Bá Đức]   | API Backend – `jobs.php`,                            | Đang xử lý | Đang xây dựng endpoint tìm kiếm, lọc theo ngành nghề, địa điểm, mức lương |
| [Nguyễn Văn Đại]  | Giao diện Tìm kiếm việc làm (`timkiem.php`)          | Đang xử lý | Đang thiết kế form lọc, danh sách tin tuyển dụng dạng card, phân trang    |
| [Nguyễn Ngọc Sơn] | Giao diện Chi tiết công việc (`chitietcongviec.php`) | Đang xử lý | Đang hiển thị mô tả công việc, yêu cầu, thông tin công ty, nút ứng tuyển  |

---

## 3. Công việc đã thực hiện

- Khởi tạo cấu trúc thư mục `api/` với các endpoint cần thiết
- Viết API `jobs.php` xử lý truy vấn danh sách tin tuyển dụng
- Xây dựng layout trang `timkiem.php` với form lọc theo ngành nghề, địa điểm, mức lương
- Xây dựng layout trang `chitietcongviec.php` hiển thị thông tin chi tiết tin tuyển dụng
- Kết nối frontend (`public/`) gọi API từ `api/` qua `fetch` / AJAX

---

## 4. Vấn đề gặp phải

- API `jobs.php` chưa xử lý đầy đủ các tham số lọc kết hợp (vừa lọc ngành nghề vừa lọc địa điểm)
- Trang `timkiem.php` chưa cập nhật kết quả real-time khi thay đổi bộ lọc
- Trang `chitietcongviec.php` chưa hiển thị đúng trạng thái nút ứng tuyển khi người dùng chưa đăng nhập
- Middleware `auth.php` cần kiểm tra thêm các trường hợp token hết hạn

---

## 5. Kế hoạch ngày tiếp theo

- Hoàn thiện API `jobs.php` — xử lý đầy đủ bộ lọc kết hợp và phân trang
- Hoàn thiện trang `timkiem.php` — tích hợp gọi API và render kết quả động
- Hoàn thiện trang `chitietcongviec.php` — xử lý trạng thái đăng nhập và nút ứng tuyển
- Bắt đầu phát triển chức năng Đăng tin tuyển dụng (`dangtin.php`)
- Kiểm thử tích hợp giữa `public/` và `api/`

---

**Người lập báo cáo:** [Nguyễn Bá Đức]  
**Ngày:** 08/04/2026
